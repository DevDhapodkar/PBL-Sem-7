numpy>=1.24
scipy>=1.10
matplotlib>=3.6
streamlit>=1.30      # interactive dashboard (app.py)
rasterio>=1.3        # read real Sentinel-2 COGs (public bucket) over Nagpur
pyproj>=3.4          # lat/lon <-> UTM (tile 44QKJ) for the lake windows
# optional, for LIVE Sentinel-1 (SAR) — real S1×S2 overlay (fetch_and_overlay.py --s1 pc):
#   Microsoft Planetary Computer (anonymous, no account):
# pystac-client>=0.7
# planetary-computer>=1.0
# rioxarray>=0.15
#   or Google Earth Engine (one-time `earthengine authenticate`):
# earthengine-api>=0.1
# optional (only for the PIL-based figure size check in dev):
# pillow>=9.0
